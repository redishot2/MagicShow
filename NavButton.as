package  {
	
	import flash.display.MovieClip;
	
	
	public class NavButton extends MovieClip {
		
		
		public function NavButton(lable:String) {
			// Make the label say the correct thing
			this.buttonText.text = lable;
		}
	}
	
}
